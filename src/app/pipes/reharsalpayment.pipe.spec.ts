import { ReharsalpaymentPipe } from './reharsalpayment.pipe';

describe('ReharsalpaymentPipe', () => {
  it('create an instance', () => {
    const pipe = new ReharsalpaymentPipe();
    expect(pipe).toBeTruthy();
  });
});
