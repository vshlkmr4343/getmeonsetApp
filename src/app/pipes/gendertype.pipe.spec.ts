import { GendertypePipe } from './gendertype.pipe';

describe('GendertypePipe', () => {
  it('create an instance', () => {
    const pipe = new GendertypePipe();
    expect(pipe).toBeTruthy();
  });
});
