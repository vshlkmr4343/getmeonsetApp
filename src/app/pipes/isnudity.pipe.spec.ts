import { IsnudityPipe } from './isnudity.pipe';

describe('IsnudityPipe', () => {
  it('create an instance', () => {
    const pipe = new IsnudityPipe();
    expect(pipe).toBeTruthy();
  });
});
