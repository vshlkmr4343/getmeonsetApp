import { ViewmorePipe } from './viewmore.pipe';

describe('ViewmorePipe', () => {
  it('create an instance', () => {
    const pipe = new ViewmorePipe();
    expect(pipe).toBeTruthy();
  });
});
